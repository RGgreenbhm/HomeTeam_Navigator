# Patient Explorer - Configuration
# Copy this file to .env and fill in your credentials
# NEVER commit .env to git (it's in .gitignore)

# ==============================================================================
# REQUIRED FOR BETA
# ==============================================================================

# Spruce Health API (for patient matching and messaging)
SPRUCE_API_TOKEN=YWlkX2NIbEdqejFTMElOUGVGVHFrZW5ZaktCRDZ5VT06c2tfaE84Wk9uOG9yVThUQ2xOalNJMzl3aXpLN1E3L1JyTUtib3lkOERjZCtPbzZNc1kxK0R5RXNBPT0=
SPRUCE_ACCESS_ID=aid_cHlGjz1S0INPeFTqkenYjKBD6yU=


# Microsoft Forms (for consent collection)
# Create form first, then paste URL here
MS_FORMS_BASE_URL=https://forms.microsoft.com/r/YOUR_FORM_ID

# Default Admin Password (CHANGE THIS after first login!)
DEFAULT_ADMIN_PASSWORD=admin123

# ==============================================================================
# MICROSOFT 365 INTEGRATION (Deferred to V1.1)
# ==============================================================================

# NOTE: Azure AD App Registration is NOT required for V1.0
# The app uses delegated permissions only (user signs in with their Microsoft account)
# These settings are reserved for future V1.1 features (OneNote, SharePoint publishing)

# Azure AD tenant (for reference)
# AZURE_TENANT_ID=your_tenant_id_here

# SharePoint site URL (for reference only - not used in V1.0)
# SHAREPOINT_SITE_URL=https://southviewteam.sharepoint.com/sites/GreenClinic

# ==============================================================================
# LOCAL CONFIGURATION
# ==============================================================================

# Data file paths (relative to project root)
PATIENT_EXCEL_PATH=data/patients.xlsx

# Database (SQLite - default location)
DATABASE_URL=sqlite:///./patient_explorer.db

# Logging
LOG_LEVEL=INFO
LOG_FILE=logs/phase0.log

# ==============================================================================
# SHAREPOINT FILE SYNC (for multi-user database sharing)
# ==============================================================================

# SharePoint sync uses mapped drives or UNC paths to sync the database
# Configure the path to a shared SharePoint folder that you and your team
# have mapped to your Windows file system.
#
# Examples:
#   S:\PatientExplorer                          (mapped drive)
#   \\southviewteam.sharepoint.com\sites\GreenClinic\PatientExplorer  (UNC path)
#
# The sync is configured in-app via Admin > SharePoint Sync tab.
# These env vars are optional - the in-app configuration takes precedence.

# SHAREPOINT_SYNC_PATH=S:\PatientExplorer
# SHAREPOINT_SYNC_ENABLED=false

# ==============================================================================
# AUTOSCRIBE MODULE - Medical Note Generation
# ==============================================================================

# Azure OpenAI - Base Configuration (shared by all models)
# Get these from Azure Portal > Azure OpenAI > Your Resource > Keys and Endpoint
AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com/
AZURE_OPENAI_KEY=your_azure_openai_key_here

# Azure OpenAI - Model Deployments (names must match Azure Portal exactly)
# Check Azure Portal > Azure OpenAI > Model deployments for your deployment names
AZURE_OPENAI_DEPLOYMENT_SUMMARIZE=gpt-4.1
AZURE_OPENAI_DEPLOYMENT_TRANSCRIBE=gpt-4o-mini-transcribe
AZURE_OPENAI_DEPLOYMENT_TTS=gpt-4o-mini-tts

# Azure Speech Services (OPTIONAL - fallback for audio transcription)
# Get these from Azure Portal > Speech Services > Your Resource > Keys and Endpoint
# AZURE_SPEECH_KEY=your_azure_speech_key_here
# AZURE_SPEECH_REGION=eastus2
